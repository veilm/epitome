# AI 2040 source reconnaissance

Investigated through Chromium on CDP port 2103 at Unix timestamp `1786340150`.
This is the next small adjacent source after AI 2027; no crawl was started during
this reconnaissance.

## Approved page scope

The rendered homepage at `https://ai-2040.com/` is the canonical Plan A
scenario. Its first-party navigation exposes a PDF, an about page, footnotes, a
supplements index, and seventeen individual supplements. The supplements index
independently exposes the same seventeen entries, so the reviewed bounded scope
is 22 URL identities in `sources/ai-2040.txt`.

The one `www.ai-2040.com` link observed in article prose resolves to a path
already represented by its canonical bare-host identity. Fragment links into
the homepage and footnotes are document positions, not separate captures.
AI 2027 and outside citations remain separate sources rather than recursively
expanding this page scope.

## Rendered variants and media

The homepage is a long, mostly textual scenario with an interactive decision
tree and 12,713 rendered text characters at inspection time. The supplements
index is a distinct card layout with 21 rendered images: seventeen supplement
covers plus repeated site artwork. All images loaded at their natural sizes in
the one-off browser checks.

No YouTube, Twitter/X, iframe, or video element was present on either inspected
page. The homepage links to Spotify, Apple Podcasts, and a Buzzsprout feed for
an audio rendition. Those are external-media inventory candidates and are not
part of the 22-page capture scope. Per current direction, YouTube and Twitter/X
must not be crawled.

## Proposed capture validation

Before the 22-page batch, capture and offline-verify the homepage as a pilot.
Require the decision tree, typography, supplement navigation, and ordinary
images to replay without production-origin requests. Confirm the first-party
PDF is stored as the underlying document rather than only as Chromium's PDF
viewer shell. Then use bounded deduplication and generous delays for the
remaining pages.

## Homepage pilot

The homepage pilot completed at:

`/mnt2/capsule/epitome/ai-2040/validation/1786344706-home`

Its manifest is complete and its capture tab closed. The primary and
all-rendered-image audits both report zero omissions. Asset completion recovered
all five missing references with two-second spacing. The largest were the
first-party 14,484,354-byte AI 2040 PDF and two substantive cited reports: a
75,187,122-byte UK government PDF and a 4,988,289-byte IEA PDF. All three bodies
identify as valid PDF documents.

A 1440x900 comparison found the same 15,927 visible characters, 4,186–4,187
pixel document height, headings, ordinary image, and page layout locally and
upstream. A strict offline log recorded 11 requests, all to the temporary local
server. The audit tabs were closed afterward.

The right-hand scenario visualization is scroll-reactive upstream. Because
replay strips scripts, it preserves a static version of the final scrolled DOM
rather than replaying transitions. This is a presentation limitation rather
than a raw-data loss: the capture retains the original server response, final
hydrated DOM, JavaScript chunks, embedded unified scenario JSON, canvas/SVG
structure, and other assets needed for later reconstruction. The main text,
decision tree, and final visualization remain readable in the current replay.

The homepage's script-driven audio player is inert in static replay. A one-off
rendered inspection of the canonical Buzzsprout feed identified the direct
64-minute Plan A MP3 narrated by Rob Miles. It is recorded in
`inventories/ai-2040-audio.json` with Spotify and Apple alternates and was not
downloaded. No YouTube or Twitter/X content was crawled.

The pilot therefore supports a progressively larger page batch. Start with a
varied ten-page selection that includes the supplements index and interactive
Economic Growth Explorer before approving the final remainder.

## First varied batch

The first ten-page increment completed at:

`/mnt2/capsule/epitome/ai-2040/crawls/1786345112`

All ten manifests are complete, every capture tab closed, and the crawl reported
zero page or asset failures. Primary-image and all-rendered-image audits found
zero omissions. The captured set includes the real 14.5 MB first-party PDF and
fourteen additional valid PDF response bodies larger than 1 MB, including
substantive cited documents from the UK government, HKEX, Nvidia, OpenAI, IEA,
and Oxford.

A desktop audit covered the supplements index, FAQ, Covert AI Projects,
Verification Plan, Economic Growth Explorer, and Alignment Roadmap. The index's
22 images and every audited article image loaded at natural size. The longest
audited articles retained 90,598 and 153,826 visible characters respectively.
A strict replay log recorded 103 requests, all to the temporary local server;
no production origin was contacted. Audit tabs were closed afterward.

The Economic Growth Explorer retains its 386,917-byte captured iframe DOM, 57
canvas elements, controls and visible labels, together with the original HTML,
JavaScript, styles, and downloaded dependencies. Its live charts are drawn into
canvas and are consequently blank in script-free replay; the live controls also
do not run. This is a known interactive-presentation limitation, not loss of the
raw application or its data. A focused live comparison confirmed the expected
two-chart layout. Reconstructing executable offline applications remains a
separate hardening task from the faithful raw capture.

The pilot plus this batch cover 11 of 22 approved identities. The remaining
eleven URLs were selected through completed-capture deduplication and are ready
for one final bounded batch with a longer inter-page delay.

## Completed bounded scope

The final eleven-page remainder completed with 60-second spacing at:

`/mnt2/capsule/epitome/ai-2040/crawls/1786346593`

All eleven manifests are complete, every capture tab closed, and the crawl
reported zero failures. Repeated primary-image and all-rendered-image audits
found zero omissions. Alongside the repeated first-party AI 2040 PDF, the run
retains six substantive external PDF bodies larger than 1 MB, including papers
from Princeton, Stanford, MIT, and Nick Bostrom's site.

A 1440x900 replay audit covered AI for Epistemics, Capability Scaling Strategy,
Compute in Plan A, Economics of Plan A, Space Governance Plan, and Transparency
Plan. These pages retained 20,891–79,033 visible characters, their diagrams and
long-form layout, and zero broken images. A strict log recorded 177 requests,
all to the local replay server. The Economics article's lazy explorer frames
load their preserved 386,917-byte application DOM after scrolling into view;
as with the standalone explorer, script-free replay deliberately leaves canvas
charts and controls inert while retaining the raw application and dependencies.
Audit tabs were closed afterward.

Completed-capture selection now reports 22 archived identities and zero
remaining URLs. The approved AI 2040 page scope is therefore complete. Its
64-minute narration remains a separate inventoried import and no YouTube or
Twitter/X content was downloaded.
